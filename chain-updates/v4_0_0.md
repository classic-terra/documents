# Terra Classic - Update v4.0.0 (v14_1)

**Release Date**: 29 Mar 2026  </br>

**Chain Update Date**: Fri Apr 17 2026 14:30 UTC </br>

## Key Changes
Release: https://github.com/classic-terra/core/releases/tag/v4.0.0

### ⚠️ SDK v0.53 notice: transaction logs deprecated (compatibility update)
With the upgrade to **Cosmos SDK v0.53** (ABCI 2.0 / CometBFT), the SDK no longer constructs **`sdk.TxLogs`** by default. This is an **upstream change** introduced with ABCI 2.0.

#### What changed upstream
- `logs` and `raw_log` are **deprecated** and are **no longer constructed by default**.
- The canonical output for tx results is now **events** (protocol-level ABCI events).

References:
- Cosmos SDK PR: https://github.com/cosmos/cosmos-sdk/pull/15845
- CosmJS changelog: https://github.com/cosmos/cosmjs/blob/main/CHANGELOG.md#deprecated-4

#### What did not change
- Contracts still emit events.
- Events are still indexed and queryable.
- Explorers/indexers that already consume events are not affected.

#### ✅ Terra Classic resolution
To reduce ecosystem breakage while aligning with upstream behavior:
- Terra Classic adds a **compatibility layer on transaction queries** (`getTx`) to reconstruct **legacy `logs` from events**.
- **Broadcast responses remain upstream** (no `logs`).

Implementation:
- TxLogsMiddleware PR: https://github.com/classic-terra/core/pull/635

#### What this means / required actions (apps & integrators)
- ✅ Apps that rely on **tx queries** should continue to work, but are strongly encouraged to migrate to **events** as canonical.
- ⚠️ Apps that rely on `logs` from **broadcast** responses will be affected. Migration path:
  1) broadcast tx
  2) wait for inclusion
  3) query by hash
  4) read `events` (or reconstructed `logs` via query)

New expected usage:
- Instead of `tx.logs[0].events`, use `tx.events`.

### SDK 0.47 -> 0.53 LCD breaking changes
With the upgrade to **Cosmos SDK v0.53**, several **LCD** endpoints and response formats change. These changes were verified against current mainnet behavior on SDK 0.47 and testnet behavior on SDK 0.53. **RPC** `/tx_search` remains unchanged.

#### Confirmed LCD changes for integrators
- **TX search by height**: `GET /cosmos/tx/v1beta1/txs` now uses `?query=tx.height={height}` instead of `?events=tx.height={height}`. Pagination should use `&limit=N&page=N`.
- **IBC denom traces**: endpoints move from `/ibc/apps/transfer/v1/denom_traces` to `/ibc/apps/transfer/v1/denoms`, and the response structure changes.
- **IBC total escrow**: the endpoint path changes from `/ibc/apps/transfer/v1/denoms/{denom}/total_escrow` to `/ibc/apps/transfer/v1/total_escrow/{denom}`.
- **Slashing signing info JSON**: `missed_blocks_counter` may be omitted when its value is `0`.

#### What this means
- **RPC** `/tx_search` does not require changes.
- **LCD** clients, deposit scanners, indexers, and scripts should review any assumptions about query parameter names, endpoint paths, and JSON field presence.
- Integrators should validate these behaviors before or immediately after the chain upgrade.

Reference:
- Detailed tested notes: https://hackmd.io/LbPqfd4CTtq4hJUGLUuNJw


## Implications / Required Actions
- **Validators** must coordinate a standard upgrade at the governance-defined halt height and restart on `terrad v4.0.0`.
- **RPC/LCD/API providers + indexers** should validate post-upgrade behavior for:
  - tx query responses (especially the `logs` field)
  - any SDK 0.53 client-side changes (protobuf, endpoints, CLI output)

## Notes / Follow-ups
- Planned halt height/time (estimate): **28214400** @ **Fri Apr 17 2026 14:30 UTC**.
